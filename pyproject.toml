[project]
name = "menu-judge"
version = "0.1.0"
description = "Web application for analyzing restaurant menu images"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0,<10.0.0",
    "pytest-cov>=4.0.0,<6.0.0",
]
